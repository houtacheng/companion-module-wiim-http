# Dependencies
node_modules
.yarn/cache
.yarn/install-state.gz
.yarn/unplugged
.pnp.*

# Build output
pkg
*.tgz

# Local-only: vendor API documentation, kept out of the repo because it is
# third-party material. See README for the upstream sources.
reference/

# Local-only: previously built packages
archive/

# macOS
.DS_Store
